# Master Project Archive Index - PetConnect AI Ecosystem v3.0.0

Complete directory index listing all source code folders, documentation guides, architecture diagrams, audit reports, assets, and build artifacts.

---

## 📁 Repository Directory Index

### 1. Source Code Core (`lib/` & `backend/`)
- `lib/src/core/`: Network client (`ApiService`), secure token storage, routing (`AppRoutes`), Material 3 themes.
- `lib/src/features/`: Feature modules for `auth`, `pet_owner`, `vet`, `volunteer`, `admin`, `smart_collar`, `ai_scan`.
- `backend/apps/`: Django apps for `accounts`, `pets`, `smart_collar`, `ai_scan`, `rescue`, `admin_dashboard`.

### 2. Comprehensive Documentation Suite (`docs/`)
- All 30+ master audit reports, deployment guides, user manuals, thesis paper, and viva preparation scripts.

### 3. Build Artifacts
- `build/app/outputs/flutter-apk/app-release.apk` (Signed Release APK - 21.0 MB).
