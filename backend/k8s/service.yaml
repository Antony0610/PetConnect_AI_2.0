apiVersion: v1
kind: Service
metadata:
  name: petconnect-backend-service
  namespace: petconnect-production
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 8001
      targetPort: 8001
      name: websocket
  selector:
    app: petconnect-backend
